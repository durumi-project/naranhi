{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "node scripts/buildCasesContext.mjs && vite build",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
